# TODO, complete deployment of a git project of spring-boot getting input from the command line (port + link)


# download maven and add it to path

# download java

# download ufw if not already installed

# configure ufw to allow the spring boot app to expose itself

# run spring boot (TODO find a way to keep a file a certain max size)
